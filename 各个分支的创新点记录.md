论文声称：
synapse数据集 224*224 300轮 mutation监督模式 batch_size=16    best_dice=83.63



个人复现的基线：
synapse数据集 224*224 300轮 mutation监督模式 batch_size=16
seed=2222  best_dice=0.838059
seed=3333   best_dice=0.835013
seed=8899  best_dice= 0.834513
综上3次平均值 avg_best_dice = 0.8358616666


synapse数据集 256*256 300轮 mutation监督模式 batch_size=16
seed=2222  best_dice=0.842954
seed=3333   best_dice=0.841509
seed=8899  best_dice= 0.845107
综上3次平均值 avg_best_dice = 0.84319




try1:在 EMCAD 解码器的 MSCB 中，根据当前特征的不确定性和相邻尺度预测差异，自适应选择 1×1、3×3、5×5 多尺度分支。不确定性 + 相邻尺度差异”的自适应路由模式。固定多尺度融合无法根据局部区域的难度和跨尺度预测是否一致，自适应决定更依赖小感受野还是大感受野
synapse数据集 224*224 300轮 mutation监督模式 batch_size=16
seed=2222  best_dice=0.839665
seed=3333   best_dice=0.824269
seed=3333(未更改seed)  best_dice=0.838769
综上3次平均值 avg_best_dice =0.834234    比基线提升：-0.001627666即-0.163%    失败！！！



try4: 
当前 EMCAD 编码器、EMCAD 解码器、四路多尺度输出和既有监督方式保持为唯一 baseline。
候选模块只读取 EMCAD 已经生成并上采样到输入分辨率的最终输出 p1 与相邻尺度输出 p2，在 p1 上增加残差 logits；p4/p3/p2 和原有监督组合不改变。
主假设预先限定为：在图块大小16、选择比例0.25、其余训练协议完全相同的条件下，使用 p1/p2 类别预测分歧选择图块（disagreement）是否优于同计算预算的固定均匀选块（uniform），从而提高患者级总体 Dice。 



try5:
创新点名称：像素级可靠性多头融合
完整思路：四个头各预测logit和可靠性图，通过像素级 softmax权重融合;粗头负责整体结构，细头负责边界。
必须提供的证据：p1、固定求和、四个全局标量、像素可靠性融合四组比较，并检查置信度是否塌缩。



try6分支：
创新点名称：目标尺度课程监督
完整思路：根据GT前景面积和边界复杂度安排监督:小目标强化高分辨率头，大目标强化粗尺度结构;训练后期逐渐恢复统一监督。